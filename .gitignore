# --- Python 虚拟环境 (最关键！) ---
# 既然你在工程中创建了 .venv，绝不能把它上传到 Git
.venv/
env/
venv/
ENV/

# --- Python 运行缓存 ---
# 这些是 Python 编译生成的字节码，不属于源代码
__pycache__/
*.py[cod]
*$py.class

# --- 编辑器配置 (VS Code) ---
# 个人偏好设置不应干扰他人，但如果你想共享工作区设置，可以注释掉最后一行
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# --- 操作系统临时文件 ---
.DS_Store
Thumbs.db

# --- 依赖清单 (如果你使用 poetry 等其他工具) ---
# 注意：不要忽略 requirements.txt！它是必需的。